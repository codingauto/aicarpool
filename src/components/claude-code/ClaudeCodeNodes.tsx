'use client';

import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function ClaudeCodeNodes() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Claude Code 节点管理</CardTitle>
      </CardHeader>
      <CardContent>
        <div className="text-center py-8">
          <p className="text-gray-500">Claude Code 节点管理功能开发中...</p>
        </div>
      </CardContent>
    </Card>
  );
}