'use client';

import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function ClaudeCodeSettings() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Claude Code 设置</CardTitle>
      </CardHeader>
      <CardContent>
        <div className="text-center py-8">
          <p className="text-gray-500">Claude Code 设置功能开发中...</p>
        </div>
      </CardContent>
    </Card>
  );
}